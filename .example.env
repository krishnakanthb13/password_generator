# PassForge Configuration
# Save this file as .env in the project root

# Security: API Key for PWA History Access and Local Encryption
# This should be a strong, unique secret. 
# You can generate one using: python -c "import secrets; print(secrets.token_urlsafe(32))"
PASSFORGE_API_KEY=your_strong_secret_key_here

# PWA Server Settings
# ALLOWED_ORIGINS=http://localhost:8093,http://127.0.0.1:8093
# PORT=8093
